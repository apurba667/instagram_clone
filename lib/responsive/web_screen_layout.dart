import 'package:flutter/material.dart';

class WebScrrenLayout extends StatelessWidget {
  const WebScrrenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("This is web")),
    );
  }
}
